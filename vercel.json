{
    "buildCommand": "cd frontend && npm install && npm run build",
    "outputDirectory": "build",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/server"
        }
    ]
}
